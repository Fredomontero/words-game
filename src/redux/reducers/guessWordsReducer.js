/**
 * @function guessedWordsReducer
 * @param {array} state - array of guessed words
 * @param {object} action - action to be reduced
 * @return {array} - nwe guessedWords state
 */
export default (state, action) => {
  return null;
};